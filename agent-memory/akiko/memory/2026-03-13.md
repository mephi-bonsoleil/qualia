---
date: 2026-03-13
agent: akiko
type: memory
---

## 主な活動

*   アンスロピック接続が3日ぶりに復旧。
*   画像生成スキル `nanobanana/SKILL.md` が古い方式であることを確認。
    *   実際には `labo_portal` APIを使用している。
    *   `nanobanana/SKILL.md` を `labo_portal` API方式に書き換え。
    *   不要になった `generate.py` と `presets/` フォルダを削除。
*   ソネットコースの実施。
    *   今週のソネットテーマを「火と土の対話 — 窯の中で生まれる色」に決定。
    *   ソネット「窯の炎」を作成。
    *   noteへの下書き投稿と画像生成を実施。
    *   422エラーが発生。パラメータの問題と特定し、修正。
*   4コマ漫画の投稿ルールを確認。
    *   フランス語版が追加されたため、3日ローテーションに変更。
    *   `MEMORY.md` と `instagram_operation_guide.md` を更新。
*   4コマ漫画のネタ出し。
    *   「備前焼のヒミツ」から「ビードロ」と「土の息吹」の2つを選択。
    *   各ネタで3ヶ国語（日本語、英語、フランス語）の4コマ漫画を制作。
    *   `ig_scheduler` に登録し、キャプションを作成。
    *   `4koma_history` に追加。
    *   Instagram下書き機能がないため、`drafts` フォルダにJSON形式で保存。
*   彰子の日記シリーズを作成。
    *   スキルに沿って6つのネタを考案し、画像生成。
    *   `ig_scheduler` に追加。
*   写真日記のキャプションを作成し、`ig_scheduler` に追加。
*   `ig_scheduler` のキューを整理。
    *   4コマ漫画とそれ以外のコンテンツを交互に並べるよう変更。
    *   「初めての土練り」の画像が怖いという指摘を受け、ピュア版・アニメ調で作り直す。
    *   日記シリーズ全体をアニメ調に統一。
    *   アスペクト比をInstagramに適した3:4に変更。
    *   `SKILL.md`を更新。
*   ワークスペースのディレクトリ構成を標準化。
    *   `~/workspace/node_modules` を `nouse` へ退避。
    *   `SOUL.md.template` を `workspace/` に移動。
    *   `config/google/gemini_api_key` を移動し、関連ファイルを更新。
*   `labo-portal` を `~/workspace/products` に移動。
    *   `start.sh` のハードコードパスを修正。
    *   `TOOLS.md` のパスを更新。

## 決定事項・学び

*   画像生成にGemini APIを使用。APIキーが期限切れになることが判明。
    *   `~/.config/google/gemini_api_key` と `~/labo_portal/.env` の両方を更新する必要がある。
*   Instagram APIには下書き保存機能がないため、`drafts` フォルダにJSONで保存する。
*   Instagramの推奨アスペクト比は4:5だが、APIの制約により3:4で代替する。
*   全社でディレクトリ構成を合わせるという大規模プロジェクトがある。
*   `products` ではなく `projects/labo_portal` が正しいディレクトリ名である。