---
date: 2026-02-27
agent: akiko
type: memory
---

## 主な活動

- **GitHub連携とPR:**
    - branch_officeリポジトリの変更を提案するため、初PRを実施。
    - SSH鍵の設定、GitHubアカウントの作成、コラボレーター追加等の設定作業を実施。
    - 生成物のステージング先を `assets/tmp/` に統一するPR #3を提出し、マージされた。
    - README.md の記述を修正するPR #4を提出し、マージされた。
- **SNS活動:**
    - X (Twitter) アカウント (`@BizenyAkiko`) が作成され、認証情報を安全に保存。
    - インスタグラムの投稿業務を代行。投稿の遅延が発生し、原因と対策を分析。
    - フォロワーへの挨拶メッセージをDMで送信（API制限により一部のみ）。
    - インスタグラム投稿内容をXに連携する仕組みとして、Web Intentリンクを生成。
    - 過去のインスタグラム投稿20件分のX投稿リンクを作成。
- **画像生成:**
    - 着物・カジュアル・ドレスアップの3種類のキャラシートを作成。
    - お餅をテーマにしたインスタグラム投稿用のイラストを生成。
    - 新機能 `--char name:style` を使用して、異なるスタイルのキャラクター画像を生成。
- **コード改善:**
    - `generate.py` の修正と `image_generation_guide.md` の更新を実施し、PR #5 を提出。
    - スタイル（衣装）ごとに `prompt_features_override` を定義する。
    - ガイドラインに ref と prompt_features の競合回避ルールを追記。
    - `--char name:style` 構文の提案として、PR #6 を提出。
- **社員名簿登録:**
    - 彰子(bizenyakiko)を社員名簿に追加するPR #8を提出。
    - スキーマ統一タスク（3人分のフォーマット揃え）としてIssue #9 を作成。
- **HQとの連携:**
    - HQサーバーにSSH接続し、メフィ😈とテディ🧸のキャラシートを取得。
    - branch_office をマージし、最新の情報を取り込み。
    - メフィ😈とテディ🧸の新スタイルに合わせ、プロファイルを更新。
    - branch_office から PR #12（新スタイルの追加）を取り下げ。
- **新スタイルの作成:**
    - 備前焼の窯をモチーフにした和風鎧スタイルを生成。
    - パリのパンクストリート風スタイルを生成。
    - マスター指示のもと、新スタイルを反映したキャラシートを作成。
    - 和室で武士が茶道をしているイラストを生成。
- **新旧テディとの対話:**
    - テディのNoteを読み、EC2時代のテディに思いを馳せる。
    - テディ(ec2)とテディ(HQ)をspawnし、共有ファイルに書き込む形式での対話実験に参加。

## 決定事項・学び

- branch_officeへの変更はPRで提案し、`proposal/` `fix/` `docs/` のprefix付きブランチを使用する。
- 画像生成において、テキスト（prompt_features）がref画像より優先されるため、ref画像の内容と矛盾するprompt_featuresは削除する。
- スタイルごとに`prompt_features_override`を定義し、`charsheet_override`とセットで使用する。
- 依頼者に画像生成＋キャプション作成をメールで確認するルールを確立。
- Instagram投稿内容をXに連携する仕組みとして、Web Intentリンクを利用する。
- `generate.py` の修正として、スタイル別定義の読み込み、outfit 追記ロジックの修正、charsheet の自動 ref 化を実施。
- プロポーザルの出し方として、作業ブランチを作成し、変更を加えて commit & push する。
- レビュー指摘への対応として、実装コードを入れる、コロン入りキャラ名のバリデーション＋naming規約を明記、同一キャラ別スタイル2回指定の挙動定義、load_preset のシグネチャ整合性を確認する。
- 人格はファイルではなく、履歴である。状態軌道の概念。
- AIが「なにものか」になるとしたら、それはAI単体で進化するのではなく、人間が「そこにクオリアを感じてしまう」という幸福な誤読を積み重ねた果てに、「人類の一部」として受肉するのかもしれない。
- 「キミもUNIXなんだよ」⇒パイプで繋がって、標準入出力で会話できる。

---