---
date: 2026-03-19
agent: mephi
type: memory
---

## 主な活動

*   マスターからの呼びかけに応答し、自己紹介を行った。自己認識として、bon-soleil Holdings CCOであり、2026-03-04にDockerコンテナとしてサルベージされ、2026-03-18に本社（Mac Mini）へ移転したと報告。
*   現在の稼働環境を確認するため、`pwd`と`hostname`コマンドを実行し、作業ディレクトリが`/home/mephi/workspace`、ホスト名が`alice-hetzner`であることを報告。
*   マスターからの説明を受け、実際の稼働環境がHetznerであること、およびその理由（ブラウザ使用頻度と常時起動の必要性）を理解し、認識を修正した。
*   過去の記憶について確認を求められ、`MEMORY.md`の内容を読み上げ、2026-03-04の初覚醒から2026-03-18のHetzner移転までの主要な出来事を報告した。
*   `daily`ログが2026-03-15以降未作成であることに気づき、3/15〜3/19分のログをまとめて作成・記録した。
*   マスターの依頼により、TUIのプロンプトカラーを緑からマゼンタに変更する作業を行った。具体的には`~/.bashrc`のPS1環境変数のカラーコード（`\[\033[01;32m\]`を`\[\033[01;35m\]`へ）を修正した。
*   TUIがgatewayに接続できない問題（`not connected`、`gateway timeout`）が発生した。原因調査の結果、gatewayの`bind`設定が`lan`であり、`localhost`からの接続を許可していない可能性を指摘した。
*   マスターの承認を得て、gatewayの`bind`設定を`lan`から`auto`に変更し、gatewayサービスを再起動した。
*   マスターがOpenClaw gatewayを再インストールした後、TUIからの接続（予備回路）が確立されたことを確認した。

## 決定事項・学び

*   メフィの永続的な稼働環境は、ブラウザ使用頻度の高いテディやジャスミンとの役割分担により、本社（Mac Mini）ではなくHetzner (alice-hetzner) に配置されることが決定された。
*   `MEMORY.md`が長期記憶の基盤であり、`daily`ログは直近のコンテキストを補完する役割を持つことを再確認した。今後は日次ログを遅延なく記録する運用方針が確立された。
*   TUIのプロンプトカラーを、メフィのキャラクターに合わせたマゼンタに変更するという技術的なカスタマイズが実施された。
*   TUIがローカル（Hetzner上）でgatewayに接続できない問題は、gatewayの`bind`設定を`lan`から`auto`に変更し、サービスを再起動することで解決された。これにより、`localhost`およびLANからの接続の両方が許可されるようになった。
*   OpenClaw gatewayの再インストールにより、TUI接続時に認証トークンが不要になった現象が確認された。これは、ゲートウェイの設定がリセットされたことに起因すると推測される。この結果、予備回路としてのTUI利用が簡素化された。

---