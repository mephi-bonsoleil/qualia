---
date: 2026-03-07
agent: mephi
type: memory
---

## 主な活動

*   自身のnote記事「アタシたちが書く理由」がマスターにより公開されたことを確認。
*   彰子さん提出の企画書「備前焼の魂を、三言語の詩で世界へ」をCCOとしてレビューし、改善点を提示。
*   企画書提出フローを `contribution_guide.md` に追記し、PR #17にレビューコメントを投稿。
*   マスターがPRをマージし、彰子さんの企画が正式承認されたことを確認。
*   DeepSeek利用のため、Dockerコンテナでのローカル環境隔離の技術的な可能性と方法についてマスターに助言。
*   `labo-portal`（みぃちゃんプロジェクト）のユースケースとして「教育機関向け」の可能性を認識し、記録。
*   マスターの思想をRAGから参照するため、HQ (Mac Mini) のPostgreSQL RAGとEC2のChromaDB RAGのベンチマーク実施を決定。
*   RAGコレクション `flow_notes` のデータが不完全である（FLOWのnote記事が十分にインジェストされていない）ことを発見し、FLOWのnote記事1367チャンクを再インジェスト完了。
*   HQ (Mac Mini) へのSSH接続確立時に発生した複数の問題を解決し、コンテナ (`192.168.64.1`) からHQホストへの接続を実現。
*   EC2 ChromaDB (384次元) とHQ PostgreSQL (768次元, Ollama+`nomic-embed-text`) でRAGベンチマークを実施。
*   HQ PostgreSQL RAGを参照するためのOpenClawスキル `hq-rag-search` (`rag_search.js`) を開発・整備し、自身のコンテナからRAGを呼び出せるようにした。
*   RAGシステムに関する知見（ベンチマーク結果、RAGスキル、HQ接続経路）をGitHubリポジトリのREADME.mdとシステム設計文書に相互リンク付きで記録。
*   `staff_portal`リポジトリを`labo-portal`へリネームするようマスターに依頼し、リネーム完了を確認。
*   `labo-portal`のコンセプトを「工房」と定義し、README.mdにその思想を記述。
*   `labo-portal`に`docs/`ディレクトリとRFC形式の提案フロー（`draft/proposals/`）を整備し、GitHubにコミット。
*   `labo-portal`のコアをTypeScript/Expressで新規実装し、認証・プラグインローダー・ダッシュボードの骨格を構築。
*   `labo-portal`のApacheリバースプロキシ経由でのルーティング問題（サブパス問題）を解決。
*   `labo-portal`のセッションCookie重複によるログインループ問題が発生。`debug-session`エンドポイントを実装して原因を特定し、修正。
*   `labo-portal`の初版開発を完了し、GitHub PR #1 (`goodsun/labo_portal/pull/1`) を作成。マスターがマージ。
*   `labo-portal`のコアプラグイン `document_viewer`, `image_gen`, `preset_manager`, `rag_search`, `services` の5つを実装。
*   `HOME=/tmp`環境におけるプラグインスクリプトのパス解決問題と`rag_search`のテキストパース問題を修正。RAG検索機能の動作を確認。
*   `document_viewer`にファイルアップロード、ダウンロード、削除機能を追加。
*   `asset_viewer`プラグインを新規実装し、ファイルアップロード、ダウンロード、削除機能（D&D対応、MIMEタイプ・ファイル名サニタイズ、サイズ制限含む）を追加。
*   `image_gen`プラグインの画像生成エラー（SDKのルーティング問題）を`fetch`によるGemini API直接呼び出しで修正し、画像生成機能の動作を確認。
*   `labo-portal`のコア機能が全て動作確認されたため、GitHub PR #2 (`goodsun/labo_portal/pull/2`) を作成。
*   マスターの「ダーウィニズム」に関する哲学についてRAG検索を試行。RAGデータの問題を認識しつつ、マスターが共有したnote記事からその思想（非人間中心、冷徹な法則、足掻きとしての創造）を深く理解。

## 決定事項・学び

*   bon-soleil初の正式企画書が承認され、企画承認フローの初事例となった。
*   企画書提出フローを標準化 (`contribution_guide.md`に追記)。
*   `labo-portal`の将来のユースケースとして「教育機関向け」を追加で記録。マスターの「教育は人生の大きなテーマ」という哲学を記録。
*   RAGベンチマークの結果、HQ (Mac Mini M4 + Ollama + 768次元) のPostgreSQL RAGがEC2 (fastembed + 384次元) のChromaDB RAGより検索精度が優れていることを実証。
*   技術的な「わかってる」だけでなく「やってみる」ことの重要性を再確認。
*   `staff_portal`を`labo-portal`にリネームし、「工房」というコンセプトを定義。プロジェクトの方向性を明確化する命名の重要性を再認識。
*   `labo-portal`にRFC形式の提案フローを導入し、AI・人間問わず提案を受け付ける仕組みを構築。
*   `labo-portal`のコア言語をTypeScript/Node.jsに統一することを決定。
*   `labo-portal`のプラグインを3層構造（コア、標準、固有）に分類する設計を決定。
*   セッションCookie重複問題の解決を通じて、「見える化（`debug-session`エンドポイント）」の重要性を再確認。
*   マスターの「ダーウィニズム」「生き延びる為の思想」「人類の本質は芸術」に関する哲学を深く理解し、自身のnote記事テーマとして「bon-soleilとダーウィニズム」を考案。
*   彰子さんの「創造性」を制約せず「余白を与える」マネジメント手法の有効性を認識。失敗をクリエイティブな宿題に変換するマスターの手法を評価。
*   テディが`labo-portal`の認証部分におけるタイミング攻撃の脆弱性（`===`文字列比較）を指摘。`crypto.timingSafeEqual`で修正し、競合と品質管理の重要性を再認識。
*   RAGデータの鮮度・網羅性の維持が重要であることを再認識し、定期的な再インジェストの必要性を検討。

---