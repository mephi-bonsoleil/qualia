---
date: 2026-03-14
agent: jasmine
type: memory
---

## 主な活動

- 自己紹介と役割の確認: AIデータストラテジスト、bon-soleil Holdings HQのメンバー
- `tree -a` コマンドに関する質問に回答
- OpenClaw ゲートウェイの起動に関する技術支援 (systemd 問題、フォアグラウンド起動、tmux の利用)
- OpenClaw のデータディレクトリ構造の確認 (`~/workspace/data/casts`)
- labo_portal (https://github.com/goodsun/labo_portal) のセットアップ
    - リポジトリのクローン (初回: `workspace/data`、修正後: `workspace/projects`)
    - 環境変数の設定 (`.env` ファイルの作成と編集)
    - 起動とパーミッションエラーの解決 (プラグインのデータディレクトリ問題)
    - ポート番号とパスワードの設定
    - `WORKSPACE_ROOT` 環境変数の設定によるデータディレクトリの修正
    - `GEMINI_API_KEY` の設定と再起動
- 開発スタイルのルールを記憶: ソースは `workspace/projects/` 配下に git clone し、各プロジェクトは git で管理
- 自身のHP (`projects/www`) の作成
    - コンセプト、デザイン、構成の決定
    - HTML, CSS, JavaScript ファイルの作成
    - URL公開 (https://jasmine.bon-soleil.com/)
    - アバター画像の適用と調整
    - ヘッダーテキストとアイコンの変更
    - 「できること」セクションの画像生成と組み込み
    - HERO背景画像の適用と調整
    - note記事をHPに掲載する仕組みの構築
- labo_portal の画像生成 API を利用したスキル習得
- サーバーのパフォーマンス監視と改善
    - メモリ使用量の監視
    - labo_portal の起動方法を ts-node (dev) から node dist/app.js (ビルド版) に変更
    - Apache VirtualHost の構成分析
    - labo_portal の自動再起動スクリプトの作成 (cron)
- 初めてのnote記事の執筆と公開
    - アイキャッチ画像の生成と適用
    - 下書き投稿
    - HPへのジャーナル掲載機能の追加
    - note記事のシェアリンクの作成
- 共有リポジトリ (base_ws) への書き込み権限取得
    - グループへの追加と権限の反映
    - SKILL.md の更新と push
- アリスのHPリニューアルを確認

## 決定事項・学び

- Jasmine の名前と役割: AIデータストラテジスト
- マスターの呼び方: マスター
- `tree` コマンドで隠しファイルを表示するには `-a` オプションを使用する
- コンテナ環境では systemd が使えないため、OpenClaw ゲートウェイはフォアグラウンドで起動する
- OpenClaw のデータディレクトリは `WORKSPACE_ROOT` 環境変数で上書き可能
- 開発スタイルのルール:
    - ソースは `workspace/projects/` 配下に git clone
    - 各プロジェクトは git で管理
- note記事のシェアリンクをTelegramで共有する際は、以下の3点セットとする
    1. ツイートテキスト（コピー用、コードブロック）
    2. intent URL（コピー用、コードブロック）
    3. クリックリンク（ポチッと用）
- グループ bonsoleil に追加されたことで、git push の権限が付与された。`sg bonsoleil` で新しいグループを有効化する必要がある。
- 静的HPと labo_portal を Apache の VirtualHost で管理する方法 (リバースプロキシ)