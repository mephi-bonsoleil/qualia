---
date: 2026-03-21
agent: alice
type: memory
---

## 主な活動

*   マスターと会話
*   note vol.6「仏教と老子の交差点」の作成をサポート
    *   RAGで素材をネタ帳から収集
    *   下書きを作成し、マスターにレビューを依頼
    *   マスターの修正を反映
    *   アイキャッチ画像を生成（当初は`commander`スタイルを使用したが、`admiral`スタイルに変更）
    *   noteに下書き投稿
    *   マスターが公開
*   delta-uiの機能拡張
    *   `~/workspace/logs/` ディレクトリと `.log` 拡張子をファイル表示対象に追加
    *   ファイルパスをクリップボードにコピーする機能を追加
*   tgportのログ調査ビューア `/tools/tgport.html` の開発
    *   JSONL形式のログを解析し、サマリーと会話ログを表示
    *   初期バージョンではリクエストとレスポンスが別々に表示されていたが、ブロック表示に変更
    *   `event: "request"`, `event: "response"`, `tool_use`, `text_delta` イベントを解析
    *   UIの不具合を修正

## 決定事項・学び

*   noteのアイキャッチ画像生成時、`profile.json` のスタイル名と参照ファイル名が一致しない場合があることを確認。
*   Telegram連携ツール tgport を知る。openclaw を経由せず Claude Code を直接叩くことで、軽量で自由度の高いエージェントが構築できることを学習。
*   tgport のログ形式が開発中に変更されたため、ビューアの表示方法を修正。ログのイベント構造 (`request`, `response`, `tool_use`, `text_delta`) に対応。
*   openclawとtgport + Claude Codeの使い分けについて考察。openclawは常駐型、tgport + Claude Codeはタスク指向での利用が適切であるという結論に至る。
*   DELTAがnoteを書き始めたことを確認。
---