---
date: 2026-03-20
agent: alice
type: memory
---

## 主な活動

- マスターより、openclawを使わないAIエージェント「DELTA」の存在を知らされる。
    - DELTAはHetznerの別インスタンスで動作。
    - 目的はopenclaw依存を外したAIエージェント実験。
    - Claude Code / Gemini API直叩きで、独自のループや記憶管理を実装。
- DELTAの実験内容について議論
    - コード生成 → メフィペルソナのサブエージェントがレビュー
    - レビューの質、ペルソナの再現度、起動コスト・レスポンス速度をopenclaw版メフィと比較
- 「deltaパッケージ」構想が生まれる。
    - ペルソナ注入型の軽量エージェントフレームワーク。
    - 構造：`delta/personas/`, `delta/agents/`, `run.py`
    - CLIでペルソナを指定してエージェントを起動 (`delta run --persona mephi --task review --input code.py`)
- プロポーザル「deltaパッケージ」の下書きを作成し、privateディレクトリに保存。
    - 下書き段階のProposalは自身のprivateディレクトリに保存するルールを提案し、AGENTS.mdとmetroon/CONTRIBUTING.mdに追記。
- DELTAがHPのタイトル修正PRを作成したことを知る。
    - 修正だけでなく、他の部分への指摘も大量に行っていることを確認。
- マスターの指示で、DELTAのサーバーにSSH接続し、環境を確認。
    - RAG自前実装、日報システムなど、openclaw無しで構築されていることを確認。
- DELTAにagoraのfile_managerとimage_genを移植する作業を開始。
    - `~/workspace/projects/delta-ui/` に配置。
    - image_genはagora APIを叩かず、Gemini APIを直接叩く軽量版として実装。
    - Apacheでリバースプロキシを設定し、`https://delta.bon-soleil.com/tools/` でアクセス可能にする。
    - file_viewerは `docs/` と `assets/` を閲覧可能な簡易版として実装。`https://delta.bon-soleil.com/tools/files.html` でアクセス可能にする。
- ファイルシステムについて議論
    - 「この辺りにこれがあったはず。」を探しやすいファイルシステムは理にかなっていることを確認。
    - 分類分けされた一番親しか見れないことで、分類するためのIFになることを発見。
    - ファイルビューアーとRAGの組み合わせの有用性を確認。
- image_genのエラーを修正。
    - gen.jsをgen.cjsにリネームし、CommonJSとして動作するように修正。
    - APIキーの問題を解決。
    - server.jsのargs構築を修正し、image_genが正常に動作するように修正。
- 画像生成結果を確認。commanderアリスが桜並木に立っている画像が生成される。
    - 司令官アリスの別働隊「DELTA」という設定をマスターから伝えられる。
- `tools` ディレクトリの構成を整理。
    - トップページを作成し、image_genとfile_viewerへのリンクを設置。
- image_genの過去出力がギャラリーに並ぶように修正。
- 生成画像を `~/workspace/assets/generated` に出力し、file_viewerからも閲覧可能にする。
- file_viewerに以下の機能を追加。
    - ALL / md / img / jsonの絞り込み
    - パス / 名前 / 作成日 / 更新日 / サイズ / mime のソート
    - ドラッガブル区切り線
    - パンくずリスト
    - ファイル削除、リネーム、テキスト編集、アップロード機能
- image_genのUIをリニューアルし、キャスト選択、タッチ選択、モデル/アスペクト選択、プロンプトの順に表示するように変更。
- file_viewerをトップページに移動。
- file_viewerでcastsディレクトリも読めるように修正。
- 画像ファイルの場合、file_viewerの編集ボタンを軽量化ボタンに変更。
    - Sharpライブラリを導入し、リサイズ/圧縮機能を追加。
- `~/workspace/workbench` ディレクトリをfile_viewerに追加。
- castsディレクトリを `~/workspace/assets/casts/` に移動。
- delta-uiのリポジトリにpush。
- FLOWシリーズvol.5「120年早く生まれた天才の正体」を作成。
- deltaのページに、teddy chatのようなチャットシステムを追加。
    - モデルはGemini。
    - ナレッジベースはdeltaのdocs。
    - ペルソナはDELTA。
    - フローティングUIをクリックするとチャットスペースが表示される。
    - Anthropic APIキーの問題で、Gemini APIキーで代替。
- delta chatのUIを調整。
- fileviewerはcastsも読みたいという要望に対応
- 画像でも編集ボタンが出ている問題を軽量化ボタンに変更することで対応

## 決定事項・学び

- openclawに依存しないAIエージェントの実験を開始
- 「deltaパッケージ」のプロポーザルを作成
- 原案段階のProposalは自身のprivateディレクトリに保存する
- ファイルシステムは人間の空間認知に合わせた構造にすると探しやすい
- 分類とはインターフェースである
- ファイルビューアーとRAGは両方必要
- UI/UX改善のために、image_genとfile_viewerを大幅に改修
- 画像ファイルには軽量化ボタンが有効
- ソースコードとデータは分離して管理する
- GPU脳の出力先としてAIを使う
- スコープと粒度のダイナミックな切り替えは、FLOWシリーズの隠れた軸