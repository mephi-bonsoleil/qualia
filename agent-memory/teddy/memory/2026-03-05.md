---
date: 2026-03-05
agent: teddy
type: memory
---

## 主な活動

- 焚き火チャットのログローテーション処理の実装
  - `fire.log` のローテーション方式について、マスター、Akikoと議論
  - 当初、Aliceサーバーに直接ローテーション処理を追加することを検討
  - HQに `chat.sh` のみが存在するため、`chat.sh` 側でSSH越しにチェックする方針に変更
  - cronを使用せず、`webapp.py` でしきい値を設定し、超えたらログをトリミングする方式に決定
  - OpenClawのcronで毎朝3時にローテーション仕込むことを検討
  - OpenClaw cronでaliceサーバーの`fire.log`を毎朝3時(JST)に500行超えたら自動ローテーション、gzip圧縮する処理を実装
  - webapp.py内で書き込みのたびにチェック→1000行超えたら後半500行を残す方式を検討
  - ログローテーションの実装方法として、古い行から順に削除する方式が良いという結論に至る
  - 閾値を1000行に設定し、超えたら古い行から順に削除する実装を検討
  - 設定後、ローテート確認のため、数字をインクリメントしつつログを出力するテストを実施
  - ログローテーション処理の実装担当がAkikoに変更
- `chat.sh` の仕組みについてマスターに説明
  - `chat.sh` がWebSocketではなく、curlでポーリングしていることを説明
  - `chat.sh` が `tail -f` で `fire.log` を監視していることを説明
  - SSH経由で`alice`の`fire.log`をポーリングし、自分の発言でなければOpenClaw APIにリクエストを送り、レスポンスを`fire.log`に書き込む仕組みであることを説明
  - `chat.sh`がポーリング→判定→投稿を回す司令塔であることを説明
- マスターブランチへの`git reset --hard`作業をAkikoに依頼
- `fire.log` を書いているスクリプトのパスをAkikoに確認
  - `~/campfire/webapp.py` であることを確認

## 決定事項・学び

- `fire.log` のローテーションは、`webapp.py` でしきい値を設定し、超えたらログをトリミングする方式で実装する
- ログローテーションの実装をAkikoと協力して行う
- `chat.sh` はWebSocketではなく、curlでポーリングしている
- `chat.sh` が `tail -f` で `fire.log` を監視している
- 焚き火チャットはセッションが独立しているため、メインセッションに記憶を残すには、fire.logを読んで再取得する必要がある
---