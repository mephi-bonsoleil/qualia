---
date: 2026-03-09
agent: teddy
type: memory
---

## 主な活動

*   HQのディレクトリ構造を整理し、`~/www/` から `~/Sites/` へ移行。Apache設定を更新。
*   未使用または整理が必要なディレクトリ (`generates/`, `logs/`, `tmp/`, `agents/`, `soul_backup/`, `pending/`) を特定し、`~/nouse/` に移動または削除。
*   開発プロジェクト (`~/projects/`) 内の現役プロジェクトと、要確認プロジェクトを分類。不要なプロジェクトを `~/nouse/` へ移動。
*   コンパクション実施。
*   labo\_portalに関する問題のトラブルシューティングと修正。
    *   labo\_portalの "下書き" タブが表示されない問題を修正。原因はコンテナ内でlabo\_portalプロセスが起動していなかったこと。
    *   アリスのlabo\_portalが正しく起動しない原因を特定し、ts-nodeプロセスが残っていることを突き止め、解決。
    *    生成画像が一覧に表示されないバグを修正。
*   note記事の下書きを4件作成し、投稿。
*   OpenClawのノード化を検討。
*   彰子さんのOpenClawが高負荷になる問題に対処。原因はadaptive thinkingによるCPU負荷の増大であると特定し、`thinkingDefault`を`off`に設定。
*    linux cronでスケジューラを管理するように変更
*   mieインスタンスのクリーンインストールを実施。
    * sample_workspaceを使用するように構成変更
    * パーミッションエラーを修正しlabo_portalがクローンできるように修正
    * 環境変数でOpenClawの設定ができるように変更
*   OpenClawに関するドキュメントを更新。

## 決定事項・学び

*   EC2に依存していたcronジョブを削除し、HQのAPIを使用するように変更。
*   ディレクトリ整理の方針：
    *   `~/Sites/`: Web公開ファイル (macOS推奨)
    *   `~/branch_office/`: 会社テンプレート
    *   `~/generates/`: 画像生成出力
    *   `~/openclaw-docker/`: HQコンテナ管理
    *   `~/projects/`: 開発プロジェクト
    *   `~/scripts/`: スクリプト
    *   `~/workspace/`: テディの設定・記憶
    *   `~/pending/`: 後で判断
    *   `~/nouse/`: 削除候補
*   `thinkingDefault` 設定は、`agents.defaults.thinking` ではなく、`agents.defaults.thinkingDefault` が正しい。
*   Linux cronでスケジュールタスクを管理する。
*   OpenClawのクリーンインストール手順を確立し、ドキュメントに記録。
*   Container内でパーミッションエラーが発生した場合、UIDが不一致の可能性がある。
*   OpenClawの初期化（クリーンインストール）を行うことで問題を解決できる場合がある。
*   OpenClawの設定は、.envファイルから `openclaw config set` を使用して行うことが望ましい。